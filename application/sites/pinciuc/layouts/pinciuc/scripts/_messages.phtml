<? if (count($this->messages)): ?>
<div class="messages">
    <ul class="messages">
    <? foreach ($this->messages as $message): ?>
    <li class="<?= $message->type ?>"><?= $message->text ?></li>
    <? endforeach; ?>
    </ul>
</div>
<? endif; ?>